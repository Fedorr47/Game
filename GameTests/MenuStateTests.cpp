#include "pch.h"

/*
TEST(MainMenuStateTest, OnCreate) {
  MenuState menuState;
  menuState.on_create();
}

TEST(MainMenuStateTest, OnDestroy) {
  MenuState menuState;
  menuState.on_destroy();
}

TEST(MainMenuStateTest, Activate) {
  MenuState menuState;
  menuState.activate();
}

TEST(MainMenuStateTest, Deactivate) {
  MenuState menuState;
  menuState.deactivate();
}

TEST(MainMenuStateTest, Update) {
  MenuState menuState;
  menuState.update();
}

TEST(MainMenuStateTest, Render) {
  MenuState menuState;
  menuState.render();
}*/